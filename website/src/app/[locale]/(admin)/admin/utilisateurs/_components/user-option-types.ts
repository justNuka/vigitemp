export type ProfileOption = { id: number; name: string; description?: string | null }
export type SiteOption = { id: number; name: string }
export type GroupOption = { Id_Groupe: number; Nom_Groupe: string | null }

